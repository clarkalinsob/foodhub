import React from "react";
import { Grid } from "semantic-ui-react";

function Menus() {
    return (
        <Grid columns={1}>
            <h1>Menus</h1>
        </Grid>
    );
}

export default Menus;
